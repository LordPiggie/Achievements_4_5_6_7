cubedNumbers = {} #This is an empty dictionary

for num in range(1,11): #This defines a number set between 1 and 10

    cubedNumbers[num] = (num ** 3) #This performs a calculation on those given numbers and adds them to the above dictionary

print(cubedNumbers) #This prints the new dictionary